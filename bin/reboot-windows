#!/bin/sh

entry_id=$(efibootmgr | grep 'Windows Boot Manager' | grep --only-matching -oP '(?<=^Boot)([0-9]+)')
sudo efibootmgr --bootnext $entry_id
echo "Next boot will be to Windows"
read -p "Reboot now? [y/N] " -n 1 -r
[[ $REPLY =~ ^[Yy]$ ]] && sudo reboot
